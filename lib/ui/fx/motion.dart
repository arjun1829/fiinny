import 'package:flutter/animation.dart';

const Curve kEase = Curves.easeOutCubic;
const Duration kFast = Duration(milliseconds: 160);
const Duration kMed = Duration(milliseconds: 220);
const Duration kSlow = Duration(milliseconds: 350);
