name: gmail_parser_server
description: Dart CLI to parse Gmail transactions
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  googleapis: ^11.2.0
  google_sign_in: ^6.1.5
  http: ^1.1.0
  cloud_firestore: ^5.4.4
  firebase_core: ^3.8.0
  # add any other deps you use in gmail_service.dart
  # Example:
  # your_app_name: ^1.0.0 (if needed for shared models)

dev_dependencies:
  lints: ^2.0.0
  test: ^1.25.0
