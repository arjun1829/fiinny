flutter_native_splash:
  # Solid green — matches the Flutter SplashScreen gradient start color.
  # No image here so the native splash fades seamlessly into the Flutter
  # animated splash without a jarring logo jump.
  color: "#2E7D32"
  color_dark: "#1B5E20"

  android_12:
    color: "#2E7D32"
    color_dark: "#1B5E20"

  web: false
  ios: false
