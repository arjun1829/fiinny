# Transaction Model (`transaction_model.dart`)

**Path:** `lib/models/transaction_model.dart`

## 1. Overview
> [!WARNING]
> This appears to be a **Legacy** or **Lite** version of `Transaction`. Most new code uses `Transaction`, `ExpenseItem`, or `TransactionItem`.

## 2. Responsibilities
- **Simple Tracking:** Basic ID, Amount, Date, Category.
- **UI Assets:** Uniquely has a `bankLogo` field directly on the model, suggesting it might be used for specific UI lists that need simple icon rendering.

## 3. Status
Likely deprecated or used only in local-only / non-cloud flows. Use `ExpenseItem` for robust features.
